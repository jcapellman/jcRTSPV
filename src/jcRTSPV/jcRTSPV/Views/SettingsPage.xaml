<Page
    x:Class="jcRTSPV.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:jcRTSPV.Views"
    Foreground="White"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="#2e2e2e">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="settings" Grid.Row="0" Style="{StaticResource HeaderTextBlockStyle}" Foreground="White" Margin="10,0,0,10" />

        <ListView Grid.Row="1" ItemsSource="{Binding Settings}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}" Style="{StaticResource BaseTextBlockStyle}" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
    
    <Page.BottomAppBar>
        <CommandBar IsOpen="True" RequestedTheme="Dark">
            <AppBarButton Icon="Save" Click="btnSave_OnClick" />
        </CommandBar>
    </Page.BottomAppBar>
</Page>